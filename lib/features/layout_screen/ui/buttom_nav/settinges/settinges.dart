import 'package:flutter/material.dart';

class SettingesScreen extends StatelessWidget {
  const SettingesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SettingesScreen'));
  }
}
