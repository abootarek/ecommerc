import 'package:flutter/material.dart';

class FavoriesScreen extends StatelessWidget {
  const FavoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('FavoriesScreen'));
  }
}
