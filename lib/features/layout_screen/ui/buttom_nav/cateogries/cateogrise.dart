import 'package:flutter/material.dart';

class CateogriseScreen extends StatelessWidget {
  const CateogriseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'CateogriseScreen',
      style: TextStyle(
        fontSize: 30,
        color: Colors.red,
      ),
    ));
  }
}
